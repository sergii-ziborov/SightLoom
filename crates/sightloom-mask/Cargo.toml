[package]
name = "sightloom-mask"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Compact mask representations and morphology for SightLoom"

[features]
default = []
alloc = []
std = ["alloc"]

[dependencies]
sightloom-core = { path = "../sightloom-core", default-features = false }

[dev-dependencies]

[lints]
workspace = true
